#!/bin/sh

case $BUTTON in
    1) $TERMINAL -e calcurse;;
    2) ;;
    3) ;;
    4) ;;
    5) ;;
    6) ;;
esac

#dateandtime=$(date '+%Y %b %d (%a)')

iconcalendar=""
colorcalendar="^c#81a2be^"

printf "%s%s %s%s\n" "$colorcalendar" "$iconcalendar" "$(LC_ALL=pt_BR.utf8 date '+%Y %b %d (%a)')" "^d^"
